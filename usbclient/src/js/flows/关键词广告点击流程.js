/**
 * task_type: search_click
 * 任一步抛错时由 任务主循环.js 的「执行一条任务」截末屏：缩放 + webp 上传（与运维接口.上传过程截图 一致）。
 */
function 关键词广告点击流程(task) {
  // 打开AMG();
  // 按顺序选择环境();
  打开Chrome浏览器();
  打开亚马逊首页();
  if (!检查账号是否已登录()) {
    throw new Error("账号未登录: 页面出现 Sign in");
  }
  首页随机浏览(2, 4);
  关键词库随机搜索浏览或加购(2, 5);
  搜索并点击目标任务广告(task);
}
